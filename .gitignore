# 系统文件
.DS_Store
*.log

# Node.js
node_modules/

# TypeScript
dist/
*.tsbuildinfo

# Python
__pycache__/
*.pyc

# scmp-deploy is an independent repository; nested copies must remain absent.
.worktree-meta.json

# 独立子仓库 / 本地项目集合
shared-skills/
CtriXin-repo/
excalidraw-mcp/

# 归档后仅保留本地回执，不再纳入父仓库
tracking/
Ctri-model-switch/
.superset/

# 生成输出
out/

# 重装备份区(含 secrets tarball,绝不入仓)
mac-rebuild-keep/

# installed-skills is an independent third-party container repo (S15-PRE-C);
# its own git boundary is installed-skills/.git, never the parent
installed-skills/
